-JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
 DEVICE:
  NAME: "AT Translated Set 2 keyboard"
#-JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
# DEVICE:
#  NAME: "Gaming KB  Gaming KB  Keyboard"
